name: ForwardDiff Integration 
on:
  push:
    branches:
      - main
    tags: ['*']
  pull_request:
  workflow_dispatch:

jobs:
  Tests:
    uses: ./.github/workflows/AutoDiffIntegration.yml
    with:
      AD: ForwardDiff
