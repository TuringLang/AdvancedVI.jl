name: Zygote Integration 
on:
  push:
    branches:
      - main
    tags: ['*']
  pull_request:
  workflow_dispatch:

jobs:
  Zygote:
    uses: ./.github/workflows/AutoDiffIntegration.yml
    with:
      AD: Zygote
