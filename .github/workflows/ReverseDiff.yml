name: ReverseDiff Integration 
on:
  push:
    branches:
      - main
    tags: ['*']
  pull_request:
  workflow_dispatch:

jobs:
  Enzyme:
    uses: ./.github/workflows/AutoDiffIntegration.yml
    with:
      AD: ReverseDiff
